# multi-proj-dep-scanner
Scan a given directory for package-lock.json files. For each file found, check dependencies against a CSV of package names and versions, and report on any overlapping packages satisfying the version constraints.
